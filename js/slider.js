var value = document.getElementByID("slider").value;
var age = document.querySelectorAll("#age_list ol");

var current = 0;

makeBold(value);

function makeBold(n) {
    if (n == 1) 
        a[n].style.fontWeight = "bold";
}

